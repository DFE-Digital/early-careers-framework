<%= form_for @user, url: admin_lead_provider_user_path do |f| %>
  <%= render "form_fields", locals={f: f} %>
  <%= f.govuk_submit "Update" %>
<% end %>

<%= govuk_back_link text: "Back", href: admin_lead_provider_users_path(@lead_provider) %>
