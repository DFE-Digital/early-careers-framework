<h1 class="govuk-heading-l">Archived Participants</h1>

<%= render SearchBox.new(
  query: params[:query],
  title: "Search archive",
  hint: "Enter the participant’s ID, name, email address or TRN",
  filters: [
    {
      field: :type,
      value: params[:type],
      options: [
        OpenStruct.new(id: "", name: ""),
        OpenStruct.new(id: "early_career_teacher", name: "ECT"),
        OpenStruct.new(id: "mentor", name: "Mentor"),
      ],
    },
  ],
) %>
<%= render Admin::Archive::Table.new(relics: @relics, page: params.fetch(:page, 1)) %>
