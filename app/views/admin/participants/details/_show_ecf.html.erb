<% can_be_updated = policy(@participant_profile).update_validation_data? %>

<%=
  render Admin::ECF::Participants::Details.new(
    can_be_updated: can_be_updated,
    profile: @participant_profile,
    participant_presenter: @participant_presenter,
  )
%>
