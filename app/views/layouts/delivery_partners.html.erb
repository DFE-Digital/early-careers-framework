<%= render layout: 'layouts/width_container_wide' do %>
  <style nonce="<%= content_security_policy_nonce %>">
    .govuk-width-container {
      max-width: 1550px;
    }
  </style>
  <%= govuk_notification_banner title_text: "Important", text: "This service only provides details for participants from the 2024 cohort and earlier. Contact your lead provider if you need information for the 2025 cohort onwards." %>
  <%= yield %>
<% end %>
