<%= if filter.sorted_by?(:mentor)
      render "currently_training_sorted_by_mentor", school:, participants:
    else
      render "currently_training_sorted_by_induction_start_date", school:, participants:
    end %>
