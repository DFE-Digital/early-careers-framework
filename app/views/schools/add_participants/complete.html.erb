<% content_for :title, "ECT or mentor added" %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">

    <% if participant_profile.user == current_user %>
      <%= govuk_panel title_text: "You have been added to the #{@cohort.display_name} cohort", text: nil, classes: "govuk-!-margin-bottom-8" %>

      <h2 class="govuk-heading-m">What happens next</h2>
      <ul class="govuk-list govuk-list--bullet">
        <li>we will contact you if we need anymore details and update you on your status</li>
      </ul>

    <% else %>
      <%= govuk_panel title_text: "#{participant_profile.user.full_name} has been added as an #{format(add_participant_form.type)}", text: nil, classes: "govuk-!-margin-bottom-8" %>

      <h2 class="govuk-heading-m">What happens next</h2>
      <ul class="govuk-list govuk-list--bullet">
        <p>We’ll send them an email in the next few days. We’ll ask them to use our service to tell us their:</p>
        <li>full name</li>
        <li>teacher reference number (TRN)</li>
        <li>date of birth</li>
        <li>National Insurance number (optional)</li>
      </ul>
    <% end %>

    <div class="govuk-button-group">
      <%= govuk_link_to "View your ECTs and mentors", schools_participants_path, class: "govuk-link govuk-link--no-visited-state" %>
    </div>
  </div>
</div>
