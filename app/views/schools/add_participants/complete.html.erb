<% content_for :title, "ECT or mentor added" %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">

    <% if participant_profile.user == current_user %>
      <%= govuk_panel title_text: "#{participant_profile.user.full_name} has been added as a mentor", text: nil, classes: "govuk-!-margin-bottom-8" %>

      <h2 class="govuk-heading-m">What happens next</h2>

      <p>We’ll contact you if we need any more details, and update you on your status.</p>

    <% else %>
      <% if participant_profile.ect? %>
        <%= govuk_panel title_text: "#{participant_profile.user.full_name} has been added as an ECT", text: nil, classes: "govuk-!-margin-bottom-8" %>
      <% else %>
        <%= govuk_panel title_text: "#{participant_profile.user.full_name} has been added as a mentor", text: nil, classes: "govuk-!-margin-bottom-8" %>
      <% end %>

      <h2 class="govuk-heading-m">What happens next</h2>

      <ul class="govuk-list govuk-list--bullet">
        <li>we’ll contact them to ask for information needed to check they’re eligible for this training</li>
        <li>we’ll let you know if they’re not eligible, or if we need any further information</li>
        <li>you can track the status of these checks by <%= govuk_link_to "viewing your ECTs and mentors", schools_participants_path %> </li>
      </ul>
    <% end %>

    <div class="govuk-button-group">
      <%= govuk_link_to "View your ECTs and mentors", schools_participants_path, class: "govuk-link govuk-link--no-visited-state" %>
    </div>
  </div>
</div>
