<% content_for :title, "ECT or mentor added" %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">

    <% if participant_profile.user == current_user %>
      <%= govuk_panel title_text: "You’ve been added as a mentor", text: nil, classes: "govuk-!-margin-bottom-8" %>

      <h2 class="govuk-heading-m">What happens next</h2>

      <p class="govuk-body">We’ll send you an email that explains what information we need from you.</p>

    <% else %>
      <% if participant_profile.ect? %>
        <%= govuk_panel title_text: "#{participant_profile.user.full_name} has been added as an ECT", text: nil, classes: "govuk-!-margin-bottom-8" %>
      <% else %>
        <%= govuk_panel title_text: "#{participant_profile.user.full_name} has been added as a mentor", text: nil, classes: "govuk-!-margin-bottom-8" %>
      <% end %>

      <h2 class="govuk-heading-m">What happens next</h2>

      <% if participant_profile.ecf_participant_eligibility.present? %>
        <p class="govuk-body">We’ll let this person know you’ve registered them for ECF-based training at your school. They do not need to provide us with any more information.</p>

        <h2 class="govuk-heading-m">Eligibility for funding</h2>

        <% if participant_profile.fundable? %>
          <p class="govuk-body">Our checks have confirmed this person is eligible for funded training, and time off timetable for training and mentoring.</p>
        <% elsif participant_profile.ecf_participant_eligibility&.no_qts_reason? %>
          <p class="govuk-body"><%= participant_profile.user.full_name %> does not have qualified teacher status (QTS) yet. They need this to be eligible for funded traning.</p>
          <p class="govuk-body">We’ll keep checking their status and notify you if something changes.</p>
        <% end %>
      <% else %>
        <p class="govuk-body">We’ll let this person know you’ve registered them for ECF-based training at your school.</p>
        <p class="govuk-body">We’ll ask them to use this service to tell us their:</p>
        <ul class="govuk-list govuk-list--bullet">
          <li>full name</li>
          <li>date of birth</li>
          <li>teacher reference number (TRN) or National Insurance number</li>
        </ul>
        <p class="govuk-body">We can then confirm if they’re eligible for funded ECF-based training.</p>
      <% end %>
    <% end %>

    <%= govuk_link_to "View your ECTs and mentors", schools_participants_path, class: "govuk-link govuk-link--no-visited-state" %>
  </div>
</div>
