<div class="govuk-grid-row">
  <div class="govuk-grid-column-full">
    <% if policy(@course_lesson).edit? %>
      <%= link_to "Edit lesson content", edit_cip_years_modules_lessons_url(@course_lesson), class: "govuk-button" %>
    <% end %>
    <h1 class="govuk-heading-l"> <%= @course_lesson.title %></h1>
    <div class="gem-c-govspeak govuk-govspeak ">
      <%= @course_lesson.content_to_html.html_safe %>
    </div>
  </div>
</div>
