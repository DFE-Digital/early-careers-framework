apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cpd-ecf-migration-web-maintenance.teacherservices.cloud
spec:
  ingressClassName: nginx
  rules:
  - host: cpd-ecf-migration-web-maintenance.teacherservices.cloud
    http:
      paths:
      - pathType: ImplementationSpecific
        backend:
          service:
            name: early-careers-framework-maintenance
            port:
              number: 80
