description: "The withdrawal request for a participant"
type: object
required:
  - data
properties:
  data:
    required:
      - type
      - attributes
    properties:
      type:
        description: "The data type"
        type: string
        example: participant
      attributes:
        $ref: "#/components/schemas/ECFParticipantWithdrawal"
